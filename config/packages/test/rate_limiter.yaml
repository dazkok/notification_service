parameters:
    notification_rate_limit: 30

framework:
    rate_limiter:
        notification_api:
            policy: 'fixed_window'
            limit: '%notification_rate_limit%'
            interval: '60 minutes'
